---
title: "Salud o economía: el falso dilema"
author: "<center>[Parametría](http://parametria.com.mx/)</center>"
date: "<center>`r format(Sys.time(), '%d/%m/%Y')`</center>"
github: "Parametria/COVID-19-Opinion"
logo: "logo_gallery.png"
mail: lorenzoln@parametria.com.mx
lang: es
output:
  html_document:
    collapsed: no
    css: '04_resources/style.css'
    df_print: paged
    include:
      after_body: '04_resources/footer.html'
      in_header: '04_resources/header.html'
    number_sections: no
    theme: lumen
    toc: yes
    toc_depth: 3
    toc_float: yes
  pdf_document:
    toc: no
    toc_depth: '1'
    keep_tex: false
    extra_dependencies: ["xcolor", "fancyhdr", "graphicx"]
params: 
  view_pdf: !r knitr::is_latex_output()
  view_html: !r knitr::is_html_output()
---

```{block eval=FALSE, include=params$view_html}
<center>
<div class="pill-nav">
<a class="active" href='https://Parametria.github.io/COVID-19-Opinion/'><b>Encuesta COVID-19 Parametría</b></a>
<a href='https://Parametria.github.io/COVID-19-Opinion/RedesSociales.html'><b>Redes Sociales</b></a>
</div>


<br>
<b>
Si quiere descargar el reporte completo en pdf haga click [Aquí](https://Parametria.github.io/COVID-19-Opinion/AnalisisParametria.pdf)
</b>
<br>
<br>
Descargue el archivo RMarkdown para crear este reporte aquí
[Rmarkdown](https://github.com/Parametria/COVID-19-Opinion/blob/master/AnalisisParametria.Rmd)
</center>
<br>

***
```

# Encuesta de Opinión por Parametría
***

```{block eval=FALSE, include=params$view_pdf}

\fancypagestyle{plain}{\pagestyle{fancy}}
El desarrollo de la pandemia de COVID-19 ha suscitado cambios significativos para diversos actores en nuestras sociedades. Los gobiernos han tenido que diseñar estrategias efectivas para atender esta crisis global de salud, así como han buscado también implementar medidas de mitigación para hacer frente a la actual y futura crisis económica. En este último campo, los empresarios se han visto obligados a generar nuevas formas de trabajo que permitan reducir las consecuencias de la inminente caída en la productividad. Asimismo, los ciudadanos han debido adaptarse a una nueva dinámica social consecuencia del aislamiento, así como han tenido que ajustarse a una nueva realidad económica que- en la mayoría de los casos- es muy desfavorable. Sin duda, la evolución de esta pandemia está teniendo efectos importantes en la opinión pública que deben mantenerse monitoreados.
```

Hay que recordar que, en México, las nuevas disposiciones gubernamentales ante la emergencia restringen los trabajos demoscópicos cara a cara por tiempo indefinido, lo que subsecuentemente limitará la disponibilidad de información confiable sobre la opinión pública en el país. ^[Diversos estudios han señalado las limitaciones de las encuestas telefónicas y por internet. En países en desarrollo como México, el sesgo se concentra en el 50 % de la población con mayores servicios, urbana, de mejores ingresos y mayor escolaridad con teléfono fijo en el hogar. Mientras que, entre los sondeos por internet, la escolaridad (educación tecnológica) y la edad son dos sesgos inminentes en solo el 70 % de la población que es posible alcanzar por este medio.] Tras esta precisión, y como forma de adaptación a las nuevas medidas, Parametría realiza su medición de abril con encuestas telefónicas. Aun teniendo en cuenta los sesgos intrínsecos de este tipo de levantamiento, la información obtenida resulta muy interesante.   
<br>
\newline
En general, los entrevistados afirman que sí se han visto afectados por la pandemia del COVID-19, lo que explica su alta preocupación de contagio. La mala evaluación de la situación económica y el pesimismo futuro en el tema, aunado al mayor porcentaje de entrevistados que afirma ya no tener miembros en el hogar que trabajen o que tuvieron una disminución de los ingresos familiares, completan un panorama poco alentador para los mexicanos. Los empresarios siguen siendo los principales actores responsables de atender las posibles afectaciones salariales, así como se espera que el gobierno entregue los apoyos económicos directo a los trabajadores. Asimismo, el gobierno federal mantiene su nivel de aprobación frente a la crisis, mientras que los mexicanos se mantienen escépticos respecto a la información de los medios.  
\newpage

````{block eval = FALSE, include=params$view_html}

***
````

```{r echo=F, background="white", message = FALSE, warning = FALSE}
require(png, quietly = T)
require(readxl, quietly = T)
require(tidyverse, quietly = T)
require(stringi, quietly = T)
require(lubridate, quietly = T)
require(plotly, quietly = T)
require(htmlwidgets, quietly = T)
require(knitr, quietly = T)
require(hrbrthemes, quietly = T)
require(RColorBrewer, quietly = T)
require(htmltools, quietly = T)
require(ggpubr, quietly = T)

u <- Sys.setlocale("LC_TIME", "Spanish")

get_png <- function(filename) {
  grid::rasterGrob(png::readPNG(filename), interpolate = TRUE)
}

p3 <- ggplot(mapping = aes(x = 0:1, y = 1)) +
  theme_void() +
  annotation_custom(get_png("04_resources/logo2.png"), xmin = .8, xmax = 1)

saveWidgetFix <- function (widget,file,...) {
  ## A wrapper to saveWidget which compensates for arguable BUG in
  ## saveWidget which requires `file` to be in current working
  ## directory.
  wd<-getwd()
  on.exit(setwd(wd))
  outDir<-dirname(file)
  file<-basename(file)
  setwd(outDir);
  saveWidget(widget,file=file,...)
}
color.palette <- function(n.steps.between=NULL,
                          steps, ...){
  
  if(is.null(n.steps.between)) n.steps.between <- rep(0, (length(steps)-1))
  if(length(n.steps.between) != length(steps)-1) stop("Must have one less n.steps.between value than steps")
  
  fill.steps <- cumsum(rep(1, length(steps))+c(0,n.steps.between))
  RGB <- matrix(NA, nrow=3, ncol=fill.steps[length(fill.steps)])
  RGB[,fill.steps] <- col2rgb(steps)
  
  for(i in which(n.steps.between>0)){
    col.start=RGB[,fill.steps[i]]
    col.end=RGB[,fill.steps[i+1]]
    for(j in seq(3)){
      vals <- seq(col.start[j], col.end[j], length.out=n.steps.between[i]+2)[2:(2+n.steps.between[i]-1)]  
      RGB[j,(fill.steps[i]+1):(fill.steps[i+1]-1)] <- vals
    }
  }
  
  new.steps <- rgb(RGB[1,], RGB[2,], RGB[3,], maxColorValue = 255)
  pal <- colorRampPalette(new.steps, ...)
  return(pal)
}

colfunc <- color.palette(steps = c(brewer.pal(n = 11, "RdBu")[10],
                                    brewer.pal(n = 11, "RdBu")[7],
                                    brewer.pal(n = 11, "RdBu")[2]))
## Data

cuadros_OP <- read_excel("01_datos/parametria_Datos.xlsx")
# Transformaciones ----
names(cuadros_OP)[6:51] <- paste0("r_", names(cuadros_OP)[6:51])


cuadros_OP <- cuadros_OP %>% 
  pivot_longer(cols = starts_with("r_"), 
               names_prefix = "r_",
               names_to = "id",
               values_to = "value")%>%
  drop_na(value)%>%
  mutate(
    País = ifelse(
      País=="USA", "EEUU", País
    ),
    value = value/100,
    id = ifelse(id=="Nivel", País, id), 
    id_clean = stri_replace_first(id, regex = "[:digit:]+. ", replacement = ""),
    id_num = stri_extract_first(id, regex = "[:digit:]+"),
    text = paste0(id_clean, "\n", "Porcentaje: " ,round(value*100, 0), "%" , "\nEncuestadora: ", Encuesta ),
    prop = round(value*100),
    Porcentaje = paste0(round(value*100), "%")
  )%>%
  group_by(Pregunta)%>%
  mutate(max = max(ifelse(as.numeric(id_num)==99, NA_integer_,as.numeric(id_num) ), na.rm = T),
         color = colfunc(max)[as.numeric(id_num)],
         color = ifelse(is.na(color), "grey", color))


categorias <- unique(cuadros_OP$Categoría)
caption <- "Elaboración con datos de <a href='https://twitter.com/Parametria'>@Parametria</a>"
caption1 <- "Elaboración propia con datos de Parametría"

#colorRamp(c(brewer.pal(n = 11, "RdBu")[1], brewer.pal(n = 11, "RdBu")[11]))
```

```{block eval=FALSE, include=params$view_pdf}
Pensando en la opinión pública como uno de los actores afectados por el COVID-19, y teniendo presente que este tipo de escenarios generan cambios muchas veces difíciles de explicar, hay que tener en cuenta que la ciudadanía está sujeta ahora a ciertas variables contextuales que construyen esa opinión. Los niveles de información sobre el tema, el tiempo en cuarentena, los temores y riesgos percibidos, así como la evaluación del trabajo de los líderes ante la crisis, son sólo algunas de estas variables. En este tema, la encuesta muestra que hasta el 81% de los mexicanos entrevistados sí ha percibido cambios significativos en su vida diaria y familiar con la llegada del COVID-19. De hecho, esta percepción de impacto creció hasta 10 puntos porcentuales respecto a la medición de marzo, lo que es evidencia de la propia evolución de la pandemia en el país. (Figura 1)
```

```{block eval=FALSE, include=params$view_html}
La encuesta muestra que hasta el 81% de los mexicanos entrevistados sí ha percibido cambios significativos en su vida diaria y familiar con la llegada del COVID-19. De hecho, esta percepción de impacto creció hasta 10 puntos porcentuales respecto a la medición de marzo, lo que es evidencia de la propia evolución de la pandemia en el país. (Figura 1)
<br>

## Impacto del Coronavirus en México - Parametría
<br>
```

```{r out.width="70%",  fig.align='center', echo=F, background="white", message = FALSE, warning = FALSE, fig.cap="Impacto del Coronavirus en México - Parametría"}
pregunta1 <- "Pensando en el impacto que el coronavirus ha tenido en su vida diaria y la de su familia, ¿diría que ha cambiado mucho, algo, poco o no ha cambiado en nada su vida?"

titulo <- "Impacto del Coronavirus en México - Parametría"
subtitulo <- pregunta1

data_cono <- cuadros_OP%>%
  filter(str_detect(Categoría, "Impacto")) %>%
  mutate(Fecha = format(fecha, "%B-%Y")) %>% 
  arrange(fecha) %>% # sort
  mutate_at(vars(Fecha), ~factor(., levels=unique(.)))


ur <-   ggplot(data_cono,
               aes(x = str_wrap(id, 10), 
                   y = value,
                   fill = id, 
                   label = Porcentaje,
                   text = text)) +
  scale_y_continuous(labels = scales::percent)+
  geom_col(width = 0.5) + 
  coord_flip(clip = "off") +
  facet_grid(cols =vars(Fecha))+
  scale_fill_manual("", values = data_cono$color)+
  theme_minimal() +
  labs(title = str_wrap(titulo, 50),
       subtitle = str_wrap(subtitulo, 50),
      caption = caption1,
       x="",
       y="")
print <- ur+
         geom_label(size = 8)+
         
         theme(plot.title = element_text(size = 35, face = "bold"),
               plot.subtitle = element_text(size = 25),
               plot.caption = element_text(size = 15),
               panel.spacing.x = unit(3, "lines"),
               axis.text.x = element_text(size = 25, vjust = 0.5),
               axis.text.y = element_text(size = 15),
               legend.position = "none")

ggsave("03_graficas/param_impacto_covid.pdf", plot = gridExtra::grid.arrange(p3, print, heights = c(.1, .9)), 
       width = 15, height = 10, dpi = 100)


if(params$view_html) {
  plotly <- ggplotly(ur+theme(axis.text.x = element_text(size=8)), tooltip = c("text"),height=600)%>%
    layout(
           
           title = "",
           hovermode = "x", 
           showlegend = F, 
           annotations = list(x = 1, y = -0.1, 
                              text = caption, 
                              showarrow = F, 
                              xref='paper', 
                              yref='paper', 
                              xanchor='right', 
                              yanchor='auto', 
                              xshift=0, 
                              yshift=0,
                              font=list(size=15, color="red")))
  saveWidgetFix(plotly,libdir = "graph_dependencies", selfcontained = F,
                file="03_graficas/param_impacto_covid.html")
  div(plotly, class="myplot", align = "center")
} else if(params$view_pdf ){
  include_graphics(path ="03_graficas/param_impacto_covid.png", auto_pdf = T)
}

```

***

\newpage
Con el fin de identificar aquellas esferas en las que los ciudadanos consideran que el gobierno debe prestar más atención, la encuesta indagó entre los entrevistados por el principal problema que enfrenta el país en este momento. 
```{block eval=FALSE, include=params$view_pdf}
Cabe señalar que entre las opciones de respuesta se encuentra uno de los retos sociales más relevantes en la última década para el gobierno mexicano: la inseguridad. ^[En el tema, las estadísticas de marzo de 2020 del Sistema Nacional de Seguridad Pública (SESNSP) ubican a este mes como el periodo más violento del que se tenga registro en el gobierno López Obrador. (DE: \textcolor{blue}{\underline{https://www.forbes.com.mx/noticias-pese-cuarentena-mexico-marzo-mas-violento-9-meses/}})] Asimismo, el tema de corrupción, especialmente durante el actual gobierno, ha adquirido tal relevancia en el discurso nacional que ha sido también incluido entre las posibles respuestas a esta pregunta.
```
```{block eval=FALSE, include=params$view_pdf}
Los datos evidencian la clara división de los entrevistados entre los temas de economía y salud, dejando atrás la aún preocupante situación de inseguridad y corrupción en un lejano tercer y cuarto lugar. Es posible que, en los meses de mayo y junio, los datos sean aún más reveladores y nos permitan entender mejor cómo hemos cambiado, de qué manera enfrentamos la pandemia de manera cotidiana y cuál es nuestro saldo final. (Figura 2)
```
```{block eval=FALSE, include=params$view_html}
Los datos evidencian la clara división de los entrevistados entre los temas de economía y salud, dejando atrás la aún preocupante situación de inseguridad y corrupción en un lejano tercer y cuarto lugar. (Figura 2)
<br>

## Principal problema de México - Percepción
```

```{r out.width="70%",  fig.align='center', echo=F, background="white", message = FALSE, warning = FALSE, fig.cap="Problemas de México - Parametría"}
pregunta <- "¿Cuál considera que es el principal problema del país en este momento?"

titulo <- "Opinión de Principal Problema en México - Parametría"
subtitulo <- pregunta

data_cono <- cuadros_OP%>%
  filter(str_detect(Categoría, "Problema")) %>%
  mutate(Fecha = format(fecha, "%B-%Y")) %>% 
  arrange(fecha) %>% # sort
  mutate_at(vars(Fecha), ~factor(., levels=unique(.)))

ur <-   ggplot(data_cono,
               aes(x = str_wrap(id, 10), y = value,
                   fill =id, 
                   label = Porcentaje,
                   text = text)) +
  geom_col(width = 0.5) + 
  scale_fill_manual(values = data_cono$color)+
  theme_minimal() +
  ylim(0, 1.05) +
  labs(title = str_wrap(titulo, 50),
       subtitle = str_wrap(subtitulo, 50),
       x="",
       y="")

print <- ur+ geom_label(size = 8)+
         scale_y_percent(limits = c(0,(max(data_cono$value)+.1)))+
         theme(plot.title = element_text(size = 35, face = "bold"),
               plot.subtitle = element_text(size = 25),
               plot.caption = element_text(size = 15),
               panel.spacing.x = unit(3, "lines"),
               axis.text.x = element_text(size = 25, vjust = 0.5),
               axis.text.y = element_text(size = 15),
               legend.position = "none")

ggsave("03_graficas/problema_op_param.pdf", plot = gridExtra::grid.arrange(p3, print, heights = c(.1, .9)) , 
       width = 15, height = 10, dpi = 100)

if(params$view_html) {
  plotly <- ggplotly(ur+theme(axis.text.x = element_text(size=8)), height=600, dynamicTicks = TRUE, tooltip = c("text"))%>%
    layout(yaxis = list(tickformat = "%"),
           title = "",
           hovermode = "x", 
           showlegend = F,
           tickvalues ="", 
           annotations = list(x = 1, y = -0.283, 
                              text = caption, 
                              showarrow = F, 
                              xref='paper', 
                              yref='paper', 
                              xanchor='right', 
                              yanchor='auto', 
                              xshift=0, 
                              yshift=0,
                              font=list(size=15, color="red")))
  saveWidgetFix(plotly,libdir = "graph_dependencies", selfcontained = F,
                file="03_graficas/problema_op_param.html")
  div(plotly, class="myplot", align = "center")
} else if(params$view_pdf ){
  include_graphics(path ="03_graficas/problema_op_param.png", auto_pdf = T)
}

```

```{block eval=FALSE, include=params$view_pdf}
\newpage
Debido a que los entrevistados señalan el tema de salud como un problema que requiere primaria atención, la encuesta indaga por su preocupación de contraer el virus. Los datos muestran que 6 de cada 10 mexicanos expresa tener miedo de contagiarse de COVID-19. Cabe señalar que una revisión de este dato, respecto a la medición de marzo, muestra que el grupo de quienes se sienten vulnerables ha permanecido constante durante el tiempo. Lo anterior parece indicar que, si bien los efectos de la pandemia continúan, la información a la que se tiene acceso es muy diversa y las disposiciones gubernamentales han cambiado, la evaluación de los ciudadanos respecto a su nivel de riesgo personal no parece verse afectada por ellos. (Figura 3)
```

```{r include=params$view_pdf,  out.width="70%",  fig.align='center', echo=F, background="white", message = FALSE, warning = FALSE, fig.cap="Miedo a COVID-19 en México - Parametría"}
pregunta <- "¿Usted tiene miedo de ser contagiado con el Coronavirus?"

titulo <- "Sentimientos durante Pandemia en México"
subtitulo <- pregunta

data_cono <- cuadros_OP%>%
  filter(str_detect(Categoría, "Miedo")) %>%
  mutate(Fecha = format(fecha, "%B-%Y"))%>% 
  arrange(fecha) %>% # sort
  mutate_at(vars(Fecha), ~factor(., levels=unique(.)))%>% 
  arrange(Fecha, id)#%>%
  #mutate(Respuesta = (reorder(id, desc(id))))

ur <-   ggplot(data_cono,
               aes(x = str_wrap(id, 10), y = value,
                   fill =id, 
                   label = Porcentaje,
                   text = text)) +
  scale_y_continuous(labels = scales::percent)+
  geom_col(width = 0.5) + 
  facet_grid(cols =vars(Fecha))+
  scale_fill_manual(values = data_cono$color)+
  theme_minimal() +
  ylim(0, 1.05) +
  labs(title = str_wrap(titulo, 50),
       subtitle = str_wrap(subtitulo, 50),
       x="",
       y="") 
print <- ur + geom_label(size = 8)+
         
         theme(plot.title = element_text(size = 35, face = "bold"),
               plot.subtitle = element_text(size = 25),
               plot.caption = element_text(size = 15),
               panel.spacing.x = unit(3, "lines"),
               axis.text.x = element_text(size = 25, vjust = 0.5),
               axis.text.y = element_text(size = 15),
               legend.position = "none")

ggsave("03_graficas/miedo_op_param.pdf", plot = gridExtra::grid.arrange(p3, print, heights = c(.1, .9)), 
       width = 15, height = 10, dpi = 100)

if(params$view_html) {
  plotly <- ggplotly(ur+theme(axis.text.x = element_text(size=8)), height=600, tooltip = c("text"))%>%
    layout(
           title = "",
           hovermode = "x", 
           tickvalues ="", 
           showlegend = F,
           annotations = list(x = 1, y = 1, 
                              text = caption, 
                              showarrow = F, 
                              xref='paper', 
                              yref='paper', 
                              xanchor='right', 
                              yanchor='auto', 
                              xshift=0, 
                              yshift=0,
                              font=list(size=15, color="red")))
  saveWidgetFix(plotly,libdir = "graph_dependencies", selfcontained = F,
                file="03_graficas/miedo_op_param.html")
  div(plotly, class="myplot", align = "center")
} else if(params$view_pdf ){
  include_graphics(path ="03_graficas/miedo_op_param.png", auto_pdf = T)
}

```

***

\newpage
Acorde con las preocupaciones expresadas, la economía es otro de los principales retos a enfrentar para los mexicanos. La mitad de los entrevistados (51%) señala el mal desempeño de la economía del país durante los últimos 12 meses, y este grupo alcanza el 39% cuando se pregunta por la situación económica personal. Hay que llamar la atención en los importantes cambios registrados en estas percepciones, de un mes a otro, posiblemente explicado por los ajustes sociales y laborales derivados de la actual contingencia. Mientras que la mala evaluación nacional creció hasta 15 puntos porcentuales, la desfavorable evaluación personal ascendió 12 puntos.  (Figuras 4 y 5)
<br>
```{block eval=FALSE, include=params$view_html}
<br>

## Percepción Retrospectiva sobre Economía del País
```

```{r out.width="70%",  fig.align='center', echo=F, background="white", message = FALSE, warning = FALSE, fig.cap="Percepción Sociotrópica Retrospectiva de Economía - Parametría"}
pregunta <- "Durante los últimos 12 meses, ¿usted diría que la economía del país ha mejorado o ha empeorado?"

titulo <- "Percepción Retrospectiva sobre Economía del País - Parametría"
subtitulo <- pregunta

data_cono <- cuadros_OP%>%
  filter(str_detect(Categoría, "EcoSocioRetro")) %>%
  mutate(Fecha = format(fecha, "%B-%Y")) %>% 
  arrange(fecha) %>% # sort
  mutate_at(vars(Fecha), ~factor(., levels=unique(.)))%>%
  arrange(Fecha, id)

ur <-   ggplot(data_cono,
               aes(x = Fecha, 
                   y = value,
                   colour =id , 
                   group = id,
                   label = Porcentaje,
                   text = text)) +
  geom_line() +
  #scale_x_datetime(date_breaks = "3 week", date_labels = "%b/%y")+
  scale_color_manual(values = data_cono$color)+
  theme_minimal() +
  labs(title = str_wrap(titulo, 50),
       subtitle = str_wrap(subtitulo, 50),
       x="",
       y="")
print <-  ur+          geom_label(size = 8)+
         scale_y_percent(limits = c(0,(max(data_cono$value)+.1)))+
         theme(plot.title = element_text(size = 35, face = "bold"),
               plot.subtitle = element_text(size = 25),
               plot.caption = element_text(size = 15),
               panel.spacing.x = unit(3, "lines"),
               axis.text.x = element_text(size = 25, vjust = 0.5),
               axis.text.y = element_text(size = 15),
               legend.position = "none")
ggsave("03_graficas/EcoSocioRetro_param.pdf", plot =gridExtra::grid.arrange(p3, print, heights = c(.1, .9)),
       width = 15, height = 10, dpi = 100)

if(params$view_html) {
  plotly <- ggplotly(ur+theme(axis.text.x = element_text(size=8)), height=600, dynamicTicks = TRUE, tooltip = c("x","text"))%>%
    layout(yaxis = list(tickformat = "%"),
           title = "",
           hovermode = "x", 
           tickvalues ="", 
           showlegend = F,
           annotations = list(x = 1, y = 1, 
                              text = caption, 
                              showarrow = F, 
                              xref='paper', 
                              yref='paper', 
                              xanchor='right', 
                              yanchor='auto', 
                              xshift=0, 
                              yshift=0,
                              font=list(size=15, color="red")))
  saveWidgetFix(plotly,libdir = "graph_dependencies", selfcontained = F,
                file="03_graficas/EcoSocioRetro_param.html")
  div(plotly, class="myplot", align = "center")
} else if(params$view_pdf ){
  include_graphics(path ="03_graficas/EcoSocioRetro_param.png", auto_pdf = T)
}

```

```{block eval=FALSE, include=params$view_html}
<br>

## Percepción Retrospectiva sobre Economía Personal
<br>
```

```{r out.width="70%",  fig.align='center', echo=F, background="white", message = FALSE, warning = FALSE, fig.cap="Percepción Egotrópica Retrospectiva de Economía - Parametría"}
pregunta <- "Durante los últimos 12 meses, ¿usted diría que su economía personal ha mejorado o ha empeorado?"

titulo <- "Percepción Retrospectiva sobre Economía del País - Parametría"
subtitulo <- pregunta

data_cono <- cuadros_OP%>%
  filter(str_detect(Categoría, "EcoEgoRetro")) %>%
  mutate(Fecha = format(fecha, "%B-%Y")) %>% 
  arrange(fecha) %>% # sort
  mutate_at(vars(Fecha), ~factor(., levels=unique(.)))%>%
  arrange(Fecha, id)

ur <-   ggplot(data_cono,
               aes(x = Fecha, 
                   y = value,
                   colour =id , 
                   group = id,
                   label = Porcentaje,
                   text = text)) +
  geom_line() + 
  scale_color_manual(values = data_cono$color)+
  theme_minimal() +
  labs(title = str_wrap(titulo, 50),
       subtitle = str_wrap(subtitulo, 50),
       x="",
       y="")
print <-  ur+          geom_label(size = 8)+
         scale_y_percent(limits = c(0,(max(data_cono$value)+.1)))+
         theme(plot.title = element_text(size = 35, face = "bold"),
               plot.subtitle = element_text(size = 25),
               plot.caption = element_text(size = 15),
               panel.spacing.x = unit(3, "lines"),
               axis.text.x = element_text(size = 25, vjust = 0.5),
               axis.text.y = element_text(size = 15),
               legend.position = "none")
ggsave("03_graficas/EcoEgoRetro_param.pdf", plot =gridExtra::grid.arrange(p3, print, heights = c(.1, .9)),
       width = 15, height = 10, dpi = 100)

if(params$view_html) {
  plotly <- ggplotly(ur+theme(axis.text.x = element_text(size=8)), height=600, dynamicTicks = TRUE, tooltip = c("x","text"))%>%
    layout(yaxis = list(tickformat = "%"),
           title = "",
           hovermode = "x", 
           tickvalues ="", 
           showlegend = F,
           annotations = list(x = 1, y = 1, 
                              text = caption, 
                              showarrow = F, 
                              xref='paper', 
                              yref='paper', 
                              xanchor='right', 
                              yanchor='auto', 
                              xshift=0, 
                              yshift=0,
                              font=list(size=15, color="red")))
  saveWidgetFix(plotly,libdir = "graph_dependencies", selfcontained = F,
                file="03_graficas/EcoEgoRetro_param.html")
  div(plotly, class="myplot", align = "center")
} else if(params$view_pdf ){
  include_graphics(path ="03_graficas/EcoEgoRetro_param.png", auto_pdf = T)
}

```

```{block eval=FALSE, include=params$view_pdf}
\newpage
Sumado a lo anterior, la evaluación económica prospectiva muestra datos poco más desalentadores. Mientras que el 53% de los entrevistados evalúan negativamente el desempeño económico nacional futuro, quienes tienen una opinión desfavorable sobre su situación económica familiar futura alcanzan el 45% de la muestra. Si bien en general los mexicanos entrevistados no muestran buenas expectativas futuras, los datos sí indican un cierto optimismo sobre el desarrollo económico en los próximos 12 meses.  (Figuras 6 y 7)
```

```{r include=params$view_pdf,  out.width="70%",  fig.align='center', echo=F, background="white", message = FALSE, warning = FALSE, fig.cap="Percepción Sociotrópica Prospectiva de Economía - Parametría"}
pregunta <- "Pensando en los próximos 12 meses, ¿usted diría que la economía del país mejorará o empeorará?"

titulo <- "Percepción Prospectiva sobre Economía del País - Parametría"
subtitulo <- pregunta

data_cono <- cuadros_OP%>%
  filter(str_detect(Categoría, "EcoSocioPros")) %>%
  mutate(Fecha = format(fecha, "%B-%Y"))%>% 
  arrange(fecha) %>% # sort
  mutate_at(vars(Fecha), ~factor(., levels=unique(.))) %>%
  arrange(Fecha, id)

ur <-   ggplot(data_cono,
               aes(x = Fecha, 
                   y = value,
                   colour =id , 
                   group = id,
                   label = Porcentaje,
                   text = text)) +
  scale_y_continuous(labels = scales::percent)+
  geom_line() + 
  scale_color_manual(values = data_cono$color)+
  theme_minimal() +
  labs(title = str_wrap(titulo, 50),
       subtitle = str_wrap(subtitulo, 50),
       x="",
       y="")
print <-  ur+          geom_label(size = 8)+
         
         theme(plot.title = element_text(size = 35, face = "bold"),
               plot.subtitle = element_text(size = 25),
               plot.caption = element_text(size = 15),
               panel.spacing.x = unit(3, "lines"),
               axis.text.x = element_text(size = 25, vjust = 0.5),
               axis.text.y = element_text(size = 15),
               legend.position = "none")
ggsave("03_graficas/EcoSocioPros_param.pdf", plot =gridExtra::grid.arrange(p3, print, heights = c(.1, .9)),
       width = 15, height = 10, dpi = 100)

if(params$view_html) {
  plotly <- ggplotly(ur+theme(axis.text.x = element_text(size=8)), height=600, dynamicTicks = TRUE, tooltip = c("x","text"))%>%
    layout(
           title = "",
           hovermode = "x", 
           tickvalues ="", 
           showlegend = F,
           annotations = list(x = 1, y = 1, 
                              text = caption, 
                              showarrow = F, 
                              xref='paper', 
                              yref='paper', 
                              xanchor='right', 
                              yanchor='auto', 
                              xshift=0, 
                              yshift=0,
                              font=list(size=15, color="red")))
  saveWidgetFix(plotly,libdir = "graph_dependencies", selfcontained = F,
                file="03_graficas/EcoSocioPros_param.html")
  div(plotly, class="myplot", align = "center")
} else if(params$view_pdf ){
  include_graphics(path ="03_graficas/EcoSocioPros_param.png", auto_pdf = T)
}
```

```{r include=params$view_pdf,  out.width="70%",  fig.align='center', echo=F, background="white", message = FALSE, warning = FALSE, fig.cap="Percepción Egotrópica Prospectiva de Economía - Parametría"}
pregunta <- "Pensando en los próximos 12 meses, ¿usted diría que su economía personal mejorará o empeorará?"

titulo <- "Percepción Prospectiva sobre Economía Personal - Parametría"
subtitulo <- pregunta

data_cono <- cuadros_OP%>%
  filter(str_detect(Categoría, "EcoEgoPros")) %>%
  mutate(Fecha = format(fecha, "%B-%Y")) %>% 
  arrange(fecha) %>% # sort
  mutate_at(vars(Fecha), ~factor(., levels=unique(.)))%>%
  arrange(Fecha, id)

ur <-   ggplot(data_cono,
               aes(x = Fecha, 
                   y = value,
                   colour =id , 
                   group = id,
                   label = Porcentaje,
                   text = text)) +
  scale_y_continuous(labels = scales::percent)+
  geom_line() + 
  scale_color_manual(values = data_cono$color)+
  theme_minimal() +
  labs(title = str_wrap(titulo, 50),
       subtitle = str_wrap(subtitulo, 50),
       x="",
       y="")
print <-  ur+          geom_label(size = 8)+
         
         theme(plot.title = element_text(size = 35, face = "bold"),
               plot.subtitle = element_text(size = 25),
               plot.caption = element_text(size = 15),
               panel.spacing.x = unit(3, "lines"),
               axis.text.x = element_text(size = 25, vjust = 0.5),
               axis.text.y = element_text(size = 15),
               legend.position = "none")
ggsave("03_graficas/EcoEgoPros_param.pdf", plot =gridExtra::grid.arrange(p3, print, heights = c(.1, .9)),
       width = 15, height = 10, dpi = 100)

if(params$view_html) {
  plotly <- ggplotly(ur+theme(axis.text.x = element_text(size=8)), height=600, dynamicTicks = TRUE, tooltip = c("x","text"))%>%
    layout(
           title = "",
           hovermode = "x", 
           tickvalues ="", 
           showlegend = F,
           annotations = list(x = 1, y = 1, 
                              text = caption, 
                              showarrow = F, 
                              xref='paper', 
                              yref='paper', 
                              xanchor='right', 
                              yanchor='auto', 
                              xshift=0, 
                              yshift=0,
                              font=list(size=15, color="red")))
  saveWidgetFix(plotly,libdir = "graph_dependencies", selfcontained = F,
                file="03_graficas/EcoEgoPros_param.html")
  div(plotly, class="myplot", align = "center")
} else if(params$view_pdf ){
  include_graphics(path ="03_graficas/EcoEgoPros_param.png", auto_pdf = T)
}
```

***

<br>
\newpage
Las evaluaciones anteriores se ven reflejadas de forma más directa en el empleo e ingreso de las familias. De forma generalizada, los cambios en los datos reportados en las diferentes mediciones son evidencia del menor número de personas que reportan no estar empleadas. De hecho, la composición de la información recopilada en abril respecto a marzo es muestra de una mayor concentración de la carga monetaria en un menor número de miembros del hogar. El grupo de entrevistados que afirma que ninguna persona en su casa trabaja subió hasta 10 puntos porcentuales. (Figura 8)
\newline

```{block eval=FALSE, include=params$view_html}
<br>

## Número de personas trabajadoras por hogar
```

```{r out.width="70%",  fig.align='center', echo=F, background="white", message = FALSE, warning = FALSE, fig.cap="Número de personas trabajadoras por hogar - Parametría"}
pregunta1 <- "¿Cuántas personas en su hogar tienen un trabajo por el que reciban un ingreso?"

titulo <- "Número de personas trabajadoras por hogar"
subtitulo <- pregunta1

data_cono <- cuadros_OP%>%
  filter(str_detect(Categoría, "Trabajo")) %>%
  mutate(Fecha = format(fecha, "%B-%Y"),
         `Número de Personas` = str_wrap(id_clean, 10),
         text = paste0("Porcentaje: " ,round(value*100, 0), "%" , "\nEncuestadora: ", Encuesta ))%>%
  arrange(fecha) %>% # sort
  mutate_at(vars(Fecha), ~factor(., levels=unique(.))) %>% 
  arrange(fecha, desc(id))


ur <-   ggplot(data_cono,
               aes(x = `Número de Personas`, 
                   y = value,
                   fill = id, 
                   label = Porcentaje,
                   text = text)) +
  scale_y_continuous(labels = scales::percent)+
  geom_col(width = 0.5) + 
  coord_flip(clip = "off") +
  facet_grid(cols =vars(Fecha))+
  scale_fill_manual("", values = data_cono$color)+
  theme_minimal() +
  labs(title = str_wrap(titulo, 50),
       subtitle = str_wrap(subtitulo, 50),
      caption = caption1,
       x="",
       y="")
print <- ur+
         geom_label(size = 8)+
         
         theme(plot.title = element_text(size = 35, face = "bold"),
               plot.subtitle = element_text(size = 25),
               plot.caption = element_text(size = 15),
               panel.spacing.x = unit(3, "lines"),
               axis.text.y = element_text(size = 25, vjust = 0.5),
               axis.text.x = element_blank(),
               legend.position = "none")

ggsave("03_graficas/trabajo_param.pdf", plot = gridExtra::grid.arrange(p3, print, heights = c(.1, .9)), 
       width = 15, height = 10, dpi = 100)


if(params$view_html) {
  plotly <- ggplotly(ur+theme(axis.text.x = element_text(size=8)), tooltip = c("x","text"),height=600)%>%
    layout(
           
           title = "",
           showlegend = F, 
           annotations = list(x = 1, y = -0.1, 
                              text = caption, 
                              showarrow = F, 
                              xref='paper', 
                              yref='paper', 
                              xanchor='right', 
                              yanchor='auto', 
                              xshift=0, 
                              yshift=0,
                              font=list(size=15, color="red")))
  saveWidgetFix(plotly,libdir = "graph_dependencies", selfcontained = F,
                file="03_graficas/trabajo_param.html")
  div(plotly, class="myplot", align = "center")
} else if(params$view_pdf ){
  include_graphics(path ="03_graficas/trabajo_param.png", auto_pdf = T)
}

```

***

\newpage
En conjunción al panorama anterior, los datos muestran un significativo problema de estabilidad laboral derivado de la contingencia del COVID-19. Las diversas medidas económicas que se han implementado han generado reducciones sustantivas a la capacidad productiva de las empresas, y con ello, de sus recursos humanos. En el tema, mientras que el 29% de los entrevistados afirma que en su hogar hay alguien que ya ha perdido su empleo debido a la situación, hasta el 35% señala que, si bien aún reciben su salario, éste se redujo. (Figura 9)
\newline

```{block eval=FALSE, include=params$view_html}
<br>

## Empleo y COVID-19 en México
```

```{r out.width="70%",  fig.align='center', echo=F, background="white", message = FALSE, warning = FALSE, fig.cap="Empleo y Covid-19 México - Parametría"}


titulo <- "Afectaciones en Desempleo y Sueldos por COVID-19"
subtitulo <- "En su hogar ¿hay alguien que..."

data_cono <- cuadros_OP%>%
  ungroup() %>% 
  filter(str_detect(Categoría, "Empleo")) %>%
  mutate(id = factor(id),
         id_clean = ifelse(as.numeric(id_num) == 99,"No sabe", id_clean ),
         Pregunta = str_replace(Pregunta, "En su hogar ¿hay alguien que ", "")
         #id = factor(id, levels = rev(levels(id)))
         )%>%
  arrange(Pregunta, id)


ur <-   ggplot(data_cono,
               aes(x = reorder(str_wrap(id_clean, 10), -as.numeric(id_num)), 
                   y = value,
                   fill = id, 
                   label = Porcentaje,
                   text = text)) +
  scale_y_continuous(labels = scales::percent)+
  geom_col(width = 0.5) + 
  coord_flip(clip = "off") +
  facet_grid(cols =vars(str_wrap(Pregunta, 40)))+
  scale_fill_manual("", values = data_cono$color)+
  theme_minimal() +
  labs(title = str_wrap(titulo, 50),
       subtitle = str_wrap(subtitulo, 50),
      caption = caption1,
       x="",
       y="")
print <- ur+
         geom_label(size = 8)+
         
         theme(plot.title = element_text(size = 35, face = "bold"),
               plot.subtitle = element_text(size = 25),
               plot.caption = element_text(size = 15),
               panel.spacing.x = unit(3, "lines"),
               axis.text.y = element_text(size = 25, vjust = 0.5),
               axis.text.x = element_blank(),
               legend.position = "none")

ggsave("03_graficas/empleo_param.pdf", plot = gridExtra::grid.arrange(p3, print, heights = c(.1, .9)), 
       width = 15, height = 10, dpi = 100)


if(params$view_html) {
  plotly <- ggplotly(ur+theme(axis.text.x = element_text(size=8)), tooltip = c("text"),height=600)%>%
    layout(
           
           title = "",
           showlegend = F, 
           annotations = list(x = 1, y = -0.1, 
                              text = caption, 
                              showarrow = F, 
                              xref='paper', 
                              yref='paper', 
                              xanchor='right', 
                              yanchor='auto', 
                              xshift=0, 
                              yshift=0,
                              font=list(size=15, color="red")))
  saveWidgetFix(plotly,libdir = "graph_dependencies", selfcontained = F,
                file="03_graficas/empleo_param.html")
  div(plotly, class="myplot", align = "center")
} else if(params$view_pdf ){
  include_graphics(path ="03_graficas/empleo_param.png", auto_pdf = T)
}

```

***
```{block eval=FALSE, include=params$view_pdf}
\newpage
Este escenario laboral poco alentador puede suscitar resultados interesantes respecto a las responsabilidades y apoyos que derivan del contexto actual. Por un lado, la responsabilidad del pago de salarios se atribuye de manera inequívoca a los empresarios o patrones. Hay que señalar también el importante porcentaje que imputa esta tarea al gobierno federal.  (Figura 10) Y, por otro lado, ante la disyuntiva de entregar apoyos a los empresarios o a las personas, 8 de cada 10 entrevistados afirma que se deben otorgar directamente a las personas con necesidad. Es decir, la gente confía y prefiere la ayuda de manera personal, lejos del debate del gobierno y los empresarios sobre el plan de rescate que enfrente la crisis económica que se aproxima. (Figura 11)
```

```{r include=params$view_pdf, out.width="70%",  fig.align='center', echo=F, background="white", message = FALSE, warning = FALSE, fig.cap="Responsabilidad de Sueldos durante Pandemia - Parametría"}
pregunta1 <- "En su opinión, ¿quién es el principal responsable de pagar los sueldos de los trabajadores o empleados que se están quedando en sus casas para disminuir los contagios por el Coronavirus: las empresas o patrones, el gobierno federal, el gobierno estatal o el gobierno municipal?"

titulo <- "Responsabilidad de Sueldos durante Pandemia"
subtitulo <- pregunta1

data_cono <- cuadros_OP%>%
  filter(str_detect(Categoría, "Responsable")) %>%
  mutate(Fecha = format(fecha, "%B-%Y"),
         id = factor(id),
         id_clean = ifelse(as.numeric(id_num) == 99,"No sabe", id_clean )
         )%>%
  arrange(id)

ur <-   ggplot(data_cono,
               aes(x = reorder(str_wrap(id_clean, 10), value), 
                   y = value,
                   fill =id, 
                   label = Porcentaje,
                   text = text)) +
  scale_y_continuous(labels = scales::percent)+
  geom_col(width = 0.5) + 
  coord_flip(clip = "off") +
  scale_fill_manual(values = data_cono$color)+
  theme_minimal() +
  ylim(0, 1.05) +
  labs(title = str_wrap(titulo, 50),
       subtitle = str_wrap(subtitulo, 50),
       x="",
       y="")

print <- ur+ geom_label(size = 8)+
         
         theme(plot.title = element_text(size = 35, face = "bold"),
               plot.subtitle = element_text(size = 25),
               plot.caption = element_text(size = 15),
               panel.spacing.x = unit(3, "lines"),
               axis.text.x = element_text(size = 25, vjust = 0.5),
               axis.text.y = element_text(size = 15),
               legend.position = "none")

ggsave("03_graficas/responsable_param.pdf", plot = gridExtra::grid.arrange(p3, print, heights = c(.1, .9)) , 
       width = 15, height = 10, dpi = 100)

if(params$view_html) {
  plotly <- ggplotly(ur+theme(axis.text.x = element_text(size=8)), height=600, dynamicTicks = TRUE, tooltip = c("text"))%>%
    layout(
           title = "",
           hovermode = "x", 
           showlegend = F,
           tickvalues ="", 
           annotations = list(x = 1, y = -0.283, 
                              text = caption, 
                              showarrow = F, 
                              xref='paper', 
                              yref='paper', 
                              xanchor='right', 
                              yanchor='auto', 
                              xshift=0, 
                              yshift=0,
                              font=list(size=15, color="red")))
  saveWidgetFix(plotly,libdir = "graph_dependencies", selfcontained = F,
                file="03_graficas/responsable_param.html")
  div(plotly, class="myplot", align = "center")
} else if(params$view_pdf ){
  include_graphics(path ="03_graficas/responsable_param.png", auto_pdf = T)
}


```

```{r include=params$view_pdf, out.width="70%",  fig.align='center', echo=F, background="white", message = FALSE, warning = FALSE, fig.cap="Apoyos Económicos Durante Pandemia - Parametría"}
pregunta1 <- "Si el gobierno diera apoyos económicos en esta contingencia por el Coronavirus, ¿a quién debería dárselos: a las empresas para que ellas se los den a los trabajadores o se los debería dar directamente a los trabajadores?"

titulo <- "Apoyos Económicos Durante Pandemia"
subtitulo <- pregunta1

data_cono <- cuadros_OP%>%
  filter(str_detect(Categoría, "Apoyos")) %>%
  mutate(Fecha = format(fecha, "%B-%Y"),
         id = factor(id),
         id_clean = ifelse(as.numeric(id_num) == 99,"No sabe", id_clean )
         )%>%
  arrange(id)

ur <-   ggplot(data_cono,
               aes(x = reorder(str_wrap(id_clean, 10), value), 
                   y = value,
                   fill =id, 
                   label = Porcentaje,
                   text = text)) +
  scale_y_continuous(labels = scales::percent)+
  geom_col(width = 0.5) + 
  coord_flip(clip = "off") +
  scale_fill_manual(values = data_cono$color)+
  theme_minimal() +
  ylim(0, 1.05) +
  labs(title = str_wrap(titulo, 50),
       subtitle = str_wrap(subtitulo, 50),
       x="",
       y="")

print <- ur+ geom_label(size = 8)+
         
         theme(plot.title = element_text(size = 35, face = "bold"),
               plot.subtitle = element_text(size = 25),
               plot.caption = element_text(size = 15),
               panel.spacing.x = unit(3, "lines"),
               axis.text.x = element_text(size = 25, vjust = 0.5),
               axis.text.y = element_text(size = 15),
               legend.position = "none")

ggsave("03_graficas/apoyos_param.pdf", plot = gridExtra::grid.arrange(p3, print, heights = c(.1, .9)) , 
       width = 15, height = 10, dpi = 100)

if(params$view_html) {
  plotly <- ggplotly(ur+theme(axis.text.x = element_text(size=8)), height=600, dynamicTicks = TRUE, tooltip = c("text"))%>%
    layout(
           title = "",
           hovermode = "x", 
           showlegend = F,
           tickvalues ="", 
           annotations = list(x = 1, y = -0.283, 
                              text = caption, 
                              showarrow = F, 
                              xref='paper', 
                              yref='paper', 
                              xanchor='right', 
                              yanchor='auto', 
                              xshift=0, 
                              yshift=0,
                              font=list(size=15, color="red")))
  saveWidgetFix(plotly,libdir = "graph_dependencies", selfcontained = F,
                file="03_graficas/apoyos_param.html")
  div(plotly, class="myplot", align = "center")
} else if(params$view_pdf ){
  include_graphics(path ="03_graficas/apoyos_param.png", auto_pdf = T)
}
```
```{block eval=FALSE, include=params$view_pdf}
No cabe duda que las opiniones descritas hasta este punto están alimentadas por los altos niveles de información que, ante esta coyuntura, ahora manejan los entrevistados. De hecho, muchas de sus respuestas son reflejo tanto de algunos argumentos usados por el gobierno federal, como del constante bombardeo informativo y/o mediático. En este sentido, el presidente López Obrador ha sabido explotar esta situación. 
```
Si bien el nivel de aprobación presidencial empezó el año con una caída, éste se ha mantenido estable durante el desarrollo de la pandemia e incluso se ha recuperado. Hoy, los datos muestran que hasta el 61% de los entrevistados califica muy bien o bien la forma en que el gobierno federal ha manejado la crisis. (Figura 12) Esta mejor evaluación del trabajo de López Obrador encuentra eco en la buena percepción de las acciones gubernamentales implementadas para prevenir la propagación del virus en el país. (Figura 13)
```{block eval=FALSE, include=params$view_html}
<br>

***

## Manejo de Pandemía desde Presidencia
```
```{r out.width="70%",  fig.align='center', echo=F, background="white", message = FALSE, warning = FALSE, fig.cap="Manejo de Pandemía desde Presidencia - Parametría"}
pregunta <- "¿Cómo cree que ha manejado el gobierno del Presidente Andrés Manuel López Obrador la epidemia del Coronavirus?"

titulo <- "Manejo de Pandemía desde Presidencia"
subtitulo <- pregunta

data_cono <- cuadros_OP%>%
  filter(str_detect(Categoría, "ManejoFed")) %>%
  mutate(Fecha = format(fecha, "%B-%Y"))%>% 
  arrange(fecha) %>% # sort
  mutate_at(vars(Fecha), ~factor(., levels=unique(.))) %>%
  arrange(Fecha, id)

ur <-   ggplot(data_cono,
               aes(x = Fecha, 
                   y = value,
                   colour =id , 
                   group = id,
                   label = Porcentaje,
                   text = text)) +
  geom_line() +
  scale_color_manual("",values = data_cono$color)+
  theme_minimal() +
  labs(title = str_wrap(titulo, 50),
       subtitle = str_wrap(subtitulo, 50),
       x="",
       y="")
print <-  ur+
  geom_label(size = 8, show.legend = FALSE)+
  scale_y_percent(limits = c(0,(max(data_cono$value)+.1)))+
         theme(plot.title = element_text(size = 35, face = "bold"),
               plot.subtitle = element_text(size = 25),
               plot.caption = element_text(size = 15),
               panel.spacing.x = unit(3, "lines"),
               axis.text.x = element_text(size = 25, vjust = 0.5),
               axis.text.y = element_text(size = 15),
               legend.position = "top"
               )
ggsave("03_graficas/EcoSocioPros_param.pdf", plot =gridExtra::grid.arrange(p3, print, heights = c(.1, .9)),
       width = 15, height = 10, dpi = 100)

if(params$view_html) {
  plotly <- ggplotly(ur+theme(axis.text.x = element_text(size=8)), height=600, dynamicTicks = TRUE, tooltip = c("x","text"))%>%
    layout(yaxis = list(tickformat = "%"),
           title = "",
           hovermode = "x", 
           tickvalues ="", 
           showlegend = F,
           annotations = list(x = 1, y = 1, 
                              text = caption, 
                              showarrow = F, 
                              xref='paper', 
                              yref='paper', 
                              xanchor='right', 
                              yanchor='auto', 
                              xshift=0, 
                              yshift=0,
                              font=list(size=15, color="red")))
  saveWidgetFix(plotly,libdir = "graph_dependencies", selfcontained = F,
                file="03_graficas/EcoSocioPros_param.html")
  div(plotly, class="myplot", align = "center")
} else if(params$view_pdf ){
  include_graphics(path ="03_graficas/EcoSocioPros_param.png", auto_pdf = T)
}
```
```{block eval=FALSE, include=params$view_html}
<br>

***

## Suficiencia de Desempeño durante Pandemía desde Presidencia
```
```{r out.width="70%",  fig.align='center', echo=F, background="white", message = FALSE, warning = FALSE, fig.cap="Suficiencia de Trabajo durante Pandemía desde Presidencia - Parametría"}
pregunta1 <- "¿Usted considera que el gobierno del Presidente Andrés Manuel López Obrador está haciendo o no lo suficiente para prevenir la propagación del Coronavirus en el país?"

titulo <- "Suficiencia de Desempeño durante Pandemía desde Presidencia - Parametría"
subtitulo <- pregunta1

data_cono <- cuadros_OP%>%
  filter(str_detect(Categoría, "SuficienteFed")) %>%
  mutate(Fecha = format(fecha, "%B-%Y"),
         id_clean = ifelse(as.numeric(id_num)==99, "No sabe", id_clean))%>%
  arrange(fecha) %>% # sort
  mutate_at(vars(Fecha), ~factor(., levels=unique(.)))%>%
  arrange(fecha, id)


ur <-   ggplot(data_cono,
               aes(x = reorder(str_wrap(id_clean, 12), as.numeric(id_num)), 
                   y = value,
                   fill = id, 
                   label = Porcentaje,
                   text = text)) +
  scale_y_continuous(labels = scales::percent)+
  geom_col(width = 0.5) + 
  coord_cartesian(clip = "off") +
  facet_grid(cols =vars(Fecha))+
  scale_fill_manual("", values = data_cono$color)+
  theme_minimal() +
  labs(title = str_wrap(titulo, 50),
       subtitle = str_wrap(subtitulo, 50),
      caption = caption1,
       x="",
       y="")
print <- ur+
         geom_label(size = 8)+
         
         theme(plot.title = element_text(size = 35, face = "bold"),
               plot.subtitle = element_text(size = 25),
               plot.caption = element_text(size = 15),
               panel.spacing.x = unit(3, "lines"),
               axis.text.x = element_text(size = 25, vjust = 0.5),
               axis.text.y = element_text(size = 15),
               legend.position = "none")

ggsave("03_graficas/suficiente_param.pdf", plot = gridExtra::grid.arrange(p3, print, heights = c(.1, .9)), 
       width = 15, height = 10, dpi = 100)


if(params$view_html) {
  plotly <- ggplotly(ur+theme(axis.text.x = element_text(size=8)), tooltip = c("text"),height=600)%>%
    layout(
           
           title = "",
           hovermode = "x", 
           showlegend = F, 
           annotations = list(x = 1, y = -0.1, 
                              text = caption, 
                              showarrow = F, 
                              xref='paper', 
                              yref='paper', 
                              xanchor='right', 
                              yanchor='auto', 
                              xshift=0, 
                              yshift=0,
                              font=list(size=15, color="red")))
  saveWidgetFix(plotly,libdir = "graph_dependencies", selfcontained = F,
                file="03_graficas/suficiente_param.html")
  div(plotly, class="myplot", align = "center")
} else if(params$view_pdf ){
  include_graphics(path ="03_graficas/suficiente_param.png", auto_pdf = T)
}

```

```{block eval=FALSE, include=params$view_pdf}
\newpage
En contraste a la figura presidencial, la evaluación del tratamiento mediático muestra una división significativa en la opinión pública mexicana. Mientras que el 44% de los entrevistados considera que los medios han manejado de forma correcta el desarrollo de la pandemia del COVID-19, un 41% cree que han exagerado muchos los riesgos. Del contraste entre la medición de marzo y abril, los datos evidencian una significativa reducción en las personas que consideran que los medios han exagerado el tema, mientras que se observa un leve crecimiento de quienes creen que los medios no informan correctamente sobre este tema. Sin embargo, hay que tener en cuenta que la evolución de esta crisis global en medio del frenesí del internet y las redes sociales, han alimentado profusamente el volumen de información no científica o falsa. (Figura 14)
```
```{r include=params$view_pdf, out.width="70%",  fig.align='center', echo=F, background="white", message = FALSE, warning = FALSE, fig.cap="Suficiencia de Trabajo durante Pandemía desde Presidencia - Parametría"}
pregunta1 <- "¿Usted considera que el gobierno del Presidente Andrés Manuel López Obrador está haciendo o no lo suficiente para prevenir la propagación del Coronavirus en el país?"

titulo <- "Suficiencia de Trabajo durante Pandemía desde Presidencia - Parametría"
subtitulo <- pregunta1

data_cono <- cuadros_OP%>%
  ungroup() %>% 
  filter(str_detect(Categoría, "Medios")) %>%
  mutate(Fecha = format(fecha, "%B-%Y"),
         id = factor(id),
         id_clean = ifelse(as.numeric(id_num) == 99,"No sabe", id_clean ))%>%
  arrange(fecha) %>% # sort
  mutate_at(vars(Fecha), ~factor(., levels=unique(.)))%>%
  arrange(fecha, id)


ur <-   ggplot(data_cono,
               aes(x = reorder(str_wrap(id_clean, 15), -as.numeric(id_num)), 
                   y = value,
                   fill = id, 
                   label = Porcentaje,
                   text = text)) +
  scale_y_continuous(labels = scales::percent)+
  geom_col(width = 0.5) + 
  coord_flip(clip = "off") +
  facet_grid(cols =vars(reorder(Fecha, fecha)))+
  scale_fill_manual("", values = data_cono$color)+
  theme_minimal() +
  labs(title = str_wrap(titulo, 50),
       subtitle = str_wrap(subtitulo, 50),
      caption = caption1,
       x="",
       y="")
print <- ur+
         geom_label(size = 7)+
         
         theme(plot.title = element_text(size = 35, face = "bold"),
               plot.subtitle = element_text(size = 25),
               plot.caption = element_text(size = 15),
               panel.spacing.x = unit(3, "lines"),
               axis.text.y = element_text(size = 25, vjust = 0.5),
               axis.text.x = element_blank(),
               legend.position = "none")

ggsave("03_graficas/medios_param.pdf", plot = gridExtra::grid.arrange(p3, print, heights = c(.1, .9)), 
       width = 15, height = 10, dpi = 100)


if(params$view_html) {
  plotly <- ggplotly(ur+theme(axis.text.x = element_text(size=8)), tooltip = c("x","text"),height=600)%>%
    layout(
           title = "",
           hovermode = "x", 
           showlegend = F, 
           annotations = list(x = 1, y = -0.1, 
                              text = caption, 
                              showarrow = F, 
                              xref='paper', 
                              yref='paper', 
                              xanchor='right', 
                              yanchor='auto', 
                              xshift=0, 
                              yshift=0,
                              font=list(size=15, color="red")))
  saveWidgetFix(plotly,libdir = "graph_dependencies", selfcontained = F,
                file="03_graficas/medios_param.html")
  div(plotly, class="myplot", align = "center")
} else if(params$view_pdf ){
  include_graphics(path ="03_graficas/medios_param.png", auto_pdf = T)
}

```

***
\newpage
Más allá de cualquier mala situación económica personal o de la información que manejen, es claro que el tema de salud también sigue siendo igualmente prioritario para los mexicanos. Ante el dilema sobre las acciones que debe hacer el gobierno frente al COVID-19, el 70% de los entrevistados afirma que se tiene que dar prioridad a cuidar la salud de las personas y restringir que salgan a realizar sus trabajos, aunque esto pueda afectar la economía de las familias. Es importante también hacer hincapié en el notable crecimiento de este grupo entre la medición de marzo y la de abril. (Figura 15) 
```{block eval=FALSE, include=params$view_html}
<br>

## Acciones por tomar frente a Pandemia
```
```{r out.width="70%",  fig.align='center', echo=F, background="white", message = FALSE, warning = FALSE, fig.cap="Acciones por tomar frente a Pandemia - Parametría"}
pregunta1 <- "Actualmente existe un debate sobre las acciones que debe tomar el gobierno frente al Coronavirus. ¿Con cuál de las siguientes opciones está más de acuerdo?"

titulo <- "Acciones por tomar frente a Pandemia - Parametría"
subtitulo <- pregunta1

data_cono <- cuadros_OP%>%
  ungroup() %>% 
  filter(str_detect(Categoría, "AccionesAcuerdo")) %>%
  mutate(Fecha = format(fecha, "%B-%Y"),
         id = factor(id),
         id_clean = ifelse(as.numeric(id_num) == 99,"No sabe", id_clean ))%>%
  arrange(fecha) %>% # sort
  mutate_at(vars(Fecha), ~factor(., levels=unique(.)))%>%
  arrange(fecha, id)


ur <-   ggplot(data_cono,
               aes(x = reorder(str_wrap(id_clean, 25), as.numeric(id_num)), 
                   y = value,
                   fill = id, 
                   label = Porcentaje,
                   text = paste0("Porcentaje: ", Porcentaje))) +
  scale_y_continuous(labels = scales::percent)+
  geom_col(width = 0.5) + 
  coord_cartesian(clip = "off") +
   facet_grid(cols =vars(reorder(Fecha, fecha)))+
  scale_fill_manual("", values = data_cono$color)+
  theme_minimal() +
  labs(title = str_wrap(titulo, 50),
       subtitle = str_wrap(subtitulo, 50),
      caption = caption1,
       x="",
       y="")
print <- ur+
         geom_label(size = 7)+
         theme(plot.title = element_text(size = 35, face = "bold"),
               plot.subtitle = element_text(size = 25),
               plot.caption = element_text(size = 15),
               panel.spacing.x = unit(3, "lines"),
               axis.text.y = element_text(size = 20, vjust = 0.5),
               axis.text.x = element_blank(),
               legend.position = "none")

ggsave("03_graficas/acciones_param.pdf", plot = gridExtra::grid.arrange(p3, print, heights = c(.1, .9)), 
       width = 15, height = 10, dpi = 100)


if(params$view_html) {
  plotly <- ggplotly(ur+theme(axis.text.x = element_text(size=8)), tooltip = c("text"),height=600)%>%
    layout(#
           #
           title = "",
           showlegend = F, 
           annotations = list(x = 1, y = 0, 
                              text = caption, 
                              showarrow = F, 
                              xref='paper', 
                              yref='paper', 
                              xanchor='right', 
                              yanchor='auto', 
                              xshift=0, 
                              yshift=0,
                              font=list(size=15, color="red")))
  saveWidgetFix(plotly,libdir = "graph_dependencies", selfcontained = F,
                file="03_graficas/acciones_param.html")
  div(plotly, class="myplot", align = "center")
} else if(params$view_pdf ){
  include_graphics(path ="03_graficas/acciones_param.png", auto_pdf = T)
}

```
```{block eval=FALSE, include=params$view_pdf}
\newpage
La opinión anterior está incluso fundamentada en la pesimista evaluación prospectiva respecto a la evolución del virus en México. Los datos muestran que hasta el 73% de los mexicanos encuestados considera que lo peor de esta pandemia aún está por venir. Hay que tener en cuenta que esta mala percepción sobre el futuro se ha mantenido constante durante los últimos meses.  (Figura 16)
```
```{r include=params$view_pdf, out.width="70%",  fig.align='center', echo=F, background="white", message = FALSE, warning = FALSE, fig.cap="Sentimientos hacia futuro sobre COVID-19 - Parametría"}
pregunta1 <- "¿Cuál de las siguientes opciones describe mejor sus sentimientos sobre el coronavirus en México?"

titulo <- "Sentimientos hacia futuro sobre COVID-19 - Parametría"
subtitulo <- pregunta1

data_cono <- cuadros_OP%>%
  ungroup() %>% 
  filter(str_detect(Categoría, "Sentimiento")) %>%
  mutate(Fecha = format(fecha, "%B-%Y"),
         id = factor(id),
         id_clean = ifelse(as.numeric(id_num) == 99,"No sabe", id_clean ))%>%
  arrange(fecha) %>% # sort
  mutate_at(vars(Fecha), ~factor(., levels=unique(.)))%>%
  arrange(fecha, id)


ur <-   ggplot(data_cono,
               aes(x = reorder(str_wrap(id_clean, 25), -as.numeric(id_num)), 
                   y = value,
                   fill = id, 
                   label = Porcentaje,
                   text = text)) +
  scale_y_continuous(labels = scales::percent)+
  geom_col(width = 0.5) + 
  coord_flip(clip = "off") +
   facet_grid(cols =vars(reorder(Fecha, fecha)))+
  scale_fill_manual("", values = data_cono$color)+
  theme_minimal() +
  labs(title = str_wrap(titulo, 50),
       subtitle = str_wrap(subtitulo, 50),
      caption = caption1,
       x="",
       y="")
print <- ur+
         geom_label(size = 7)+
         
         theme(plot.title = element_text(size = 35, face = "bold"),
               plot.subtitle = element_text(size = 25),
               plot.caption = element_text(size = 15),
               panel.spacing.x = unit(3, "lines"),
               axis.text.y = element_text(size = 20, vjust = 0.5),
               axis.text.x = element_blank(),
               legend.position = "none")

ggsave("03_graficas/sentimientos_param.pdf", plot = gridExtra::grid.arrange(p3, print, heights = c(.1, .9)), 
       width = 15, height = 10, dpi = 100)


if(params$view_html) {
  plotly <- ggplotly(ur+theme(axis.text.x = element_text(size=8)), tooltip = c("x","text"),height=600)%>%
    layout(
           title = "",
           hovermode = "x", 
           showlegend = F, 
           annotations = list(x = 1, y = -0.1, 
                              text = caption, 
                              showarrow = F, 
                              xref='paper', 
                              yref='paper', 
                              xanchor='right', 
                              yanchor='auto', 
                              xshift=0, 
                              yshift=0,
                              font=list(size=15, color="red")))
  saveWidgetFix(plotly,libdir = "graph_dependencies", selfcontained = F,
                file="03_graficas/sentimientos_param.html")
  div(plotly, class="myplot", align = "center")
} else if(params$view_pdf ){
  include_graphics(path ="03_graficas/sentimientos_param.png", auto_pdf = T)
}

```

\newpage

***
Si bien es aventurado buscar prever el final y el grado de impacto de la pandemia del COVID-19, no cabe duda que habrá costos sociales, económicos, e incluso políticos muy altos. 
```{block eval=FALSE, include=params$view_pdf}
Los actores se han visto forzados a diseñar e implementar estrategias de mitigación a la actual y futura crisis, incluyendo el trabajo de la ciudadanía. Sin embargo, los efectos de esta pandemia dejarán huellas significativas en el desarrollo social y económico del país en el próximo lustro, y, en ese nuevo escenario, la opinión pública estará buscando responsables por la crisis.
```
Este es finalmente un dilema sin solución óptima y en el peor de los casos ambas crisis nos pasarán factura. 

<br>
\newpage

# Nota metodológica

| Aplicación de encuesta vía telefónica en CATI | Modalidad HOME-CATI llamadas realizadas desde casa|
|:------|------:|
| Representatividad | Hogares del país con teléfono en su vivienda | 
| Población objetivo | Personas de 18 años en adelante residentes del país con teléfono en vivienda |
|Número de entrevistas| 420 encuestas | 
| Fecha de levantamiento 1ra ola | 28-29 de marzo de 2020|
| Fecha de levantamiento 2da ola | 23-24 de abril de 2020|
| Nivel de confianza estadística | 95%. Margen de error: (+/-) 4.8% |
| Diseño, muestreo, aplicación de entrevistas y análisis | Parametría SA de CV |
| Método de muestreo | Aleatorio simple |

```{block eval=FALSE, include=params$view_html}
# Info
***

<br>
Revise el acervo histórico de reportes [aquí](https://github.com/Parametria/COVID-19-Opinion/archivo_historico).
<br>
Descargue el archivo RMarkdown para crear este reporte aquí
[Rmarkdown](https://github.com/Parametria/COVID-19-Opinion/)
<br>
También puedes descargar el tema de este Rmd [aquí](https://holtzy.github.io/Pimp-my-rmd/).
```
```{block eval=FALSE, include=params$view_pdf}
Descargue los archivo RMarkdown para crear este reporte aquí [\underline{Rmarkdown}](https://github.com/Parametria/COVID-19-Opinion).
```